// here we gonna create the declaration

#ifndef COMPLEX_MATH_H
#define COMPLEX_MATH_H

// we can also do nested namespaces
namespace complexMath::arithmetic
{
    int multiplication(int a, int b);
}

namespace complexMath::geometry
{
    int rectangePerimeter(int a, int b);
}

#endif